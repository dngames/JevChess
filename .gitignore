# The API key lives here and must never be committed.
.env

# Node
node_modules/
npm-debug.log*
*.log

# Experiment output (regenerated by tools/experiment.mjs and tools/selfplay.mjs)
experiments/

# Local tool caches: machine state, not project state.
# (.wrangler/ got committed once by `git add -A`; tests/repo-hygiene.test.mjs now guards this.)
.wrangler/
.vercel/
.netlify/
.dev.vars

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
